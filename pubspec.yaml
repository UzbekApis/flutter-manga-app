name: manga_reader
description: Manga o'qish ilovasi
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP so'rovlar
  http: ^1.1.0
  
  # State management
  provider: ^6.1.1
  
  # Local storage
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  
  # Rasm cache
  cached_network_image: ^3.3.0
  
  # Rasm ko'rish
  photo_view: ^0.14.0
  
  # Yuklab olish
  dio: ^5.4.0
  
  # Permission
  permission_handler: ^11.1.0
  
  # UI
  flutter_spinkit: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
